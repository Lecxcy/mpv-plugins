# 故意不绑给 mpv-multi 的 UTI。set-video-handlers.sh --check 反查"清单外漏网"
# 时会跳过这里列出的，免得每次校验都报一条永远不会去修的告警。
#
# 只放"明确决定不要"的，不要拿它掩盖真正的漏网。

# .mp4 的音频侧 UTI。它同时也是 .m4a 的 UTI，绑了会把纯音频文件一起抢走。
# .mp4 视频本身走 public.mpeg-4，不受影响。音频目前整体留给系统默认。
public.mpeg-4-audio
